# ART-02-NEW · 水上冲关模型交付

状态：同款机械零件与固定名资源已准备，正在按用户要求重排弯绕路线；旧长条布局不是最终交付。新关卡只使用自己的正式 `art/water-rush.blend` 和固定名资源；首次创建后就地维护，不建candidate/版本副本或备份，第一关资源不改。当前真实入口为127.0.0.1:5177，浏览器与玩法验证由代码对话独占。

## 视觉与分件方式

用户已收窄为“和第一关一样，只是加障碍物”。直接复用第一关象牙台面、.065米倒角、连续深灰底座和橙色夹层、橡胶脚垫/合金脚座/套环/锚钉/斜撑、橙栏固定件与材质。转盘和升降板是同系列新增机械部件，不另做水上闯关主题分区或重配色。径向细线只用于看清转盘旋转；升降板边带为原有平台语义，不增加假护栏。池水只沿用既有环境，不制作池体GLB。

可编辑场景按静态轨道/落脚岛、坡面、转盘、升降板、复用机关分组。池水、运行时圆环与警示线不烘焙到GLB。转盘与升降板须独立原点和实例；真实碰撞/承托/旋转作用由代码实现，模型不包含玩法动画或脚本。

静态尺寸等LEVEL第一份布局确认后直接排布；动态部件的米制原点、轴向与分件边界以CODE实际契约为准。首次场景是可编辑模块，最终配置冻结后再导出water-rush-track.glb及必要water-rush-turntable.glb/water-rush-lift.glb。现有平端锤头、柄和横移平台优先原样复用，不复制同内容GLB。

## 本轮保护

玩家没有跳跃键。可见桥面/坡面必须与真实代理一致；护栏端点止于转角，圆环、落脚岛和机关完整扫掠预留空间。转盘不能只旋转贴图，升降板不通过粘球补通行，冲坡不使用隐藏速度阈值；上述验收需要CODE真实运行证据，不以Blender预览代替。

## 当前落盘与最新路线变更

- 可编辑场景：`art/water-rush.blend`，单一正式文件，无版本副本/备份；Layout controls保留每块台面/坡面及底座层的独立布尔操作数，静态表面并集去除重叠内面。
- 资产准备：`public/models/water-rush-track.glb`、`water-rush-turntable.glb`、`water-rush-lift.glb`，按首份CODE协议原点导出，但仍待新弯绕布局重接和运行时实际支持。第一关锤和横移台仅引用，未复制输出。
- 当前`docs/art/water-rush-preview.png`与报告仍对应调整前单列布局；用户已要求机关间S/U折返，不可用此图宣称最终完成。等待LEVEL新俯视坐标后直接移动/重接当前文件，继续交完整俯视和斜视预览。
- 当前已具备独立转盘（盘心原点，含一根有匹配代理的挡杆）和共用升降板（盒中心原点，含细伸缩杆）。板下杆最低1.21米，固定中空套筒顶2.30米；内半径.095、杆半径.075，留径向.02米，最低板腹2.41米与套筒顶相隔.11米。此为模型净隙设计，不证明引擎承托/接缝通行。
