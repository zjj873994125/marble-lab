# 第二关：水上冲关

最终提交与推送授权：用户明确要求本批全部改动完成后，由统筹直接执行 git add、commit、push 到远程 main，不再询问权限。用户随后在代码任务明确“不用检查了，继续开发”，因此不再以自动测试、速度对照或试玩通过作为开发/提交前置；如实注明本次未检验的功能与难度，不冒充通过。发布范围包括第二关最新蛇形/细梁/坡道、最终模型与数据、手机操作及相关说明。三方完成后回传准确文件与未完项，由统筹读取 Git 状态/差异确认提交范围并统一提交推送，避免并发 Git 写。现有 GitHub CI 不删除，推送后自动运行；保护无关用户改动，不强推、不回滚、不操作服务器。

日期：2026-09-11。当前最高优先级：用户要求推进第二关，代码立即实施实际接入，不再等待第一关独立美化或补测。第一关现状和有效证据保留，其余独立待办后置；仅做共享改动必要的第一关回归。下文历史“代码暂停/待恢复/先收尾第一关”均不再生效。Git/CI/CD 保留，服务器仍由用户部署，三方按最新三锤/十字对齐/折返布局配套。

并行重要需求：[手机摇杆、刹车与横屏 HUD](mobile-controls.md)。代码负责两关共用触控能力，关卡/美术继续第二关调难与S弯/独木桥几何；等待配套数据时可先做触控，最终共同验收，不取消任何已明确的第二关目标。

当前实际里程碑：本批开发与模型配套已完成，serpentine 使用R3.5/宽1.6、四次交替180°回头与入口90°、.5米桥、向-Z的50°/4.4米坡；同名静态模型引用已恢复，手机暂停说明与小踏板收口。统筹按已授权范围统一提交推送main。用户已取消本轮测试/试玩，新几何和最终玩法未经本轮验证，不沿用旧R8证据；提交和CI结果以Git历史及统筹最终回报为准。

最新路线修正：用户明确否定当前障碍一列排开的长条预览，要求在障碍物之间弯绕过渡。现有首份直线布局不再作为最终排布；由关卡在原设计文件中重排为紧凑的多次折返路线，美术依新布局原位调整当前 .blend，不改第一关、不建布局版本或备份。

最新机关结构修正：图中第二关大锤应使用跨赛道门架，两根方柱在行进方向左右两侧、横梁跨赛道，吊点在横梁中间且位于锤头中位正上方；默认静止姿态垂直居中，然后向两侧对称摆动。旧同侧支架/偏置吊点不作为最终方案，具体要求见文末。

最新数量与节奏：用户要求第一段改为三把大摆锤，摆动方向和速度错开。三组均沿用跨道门架与居中静止姿态，分别有独立速度、相位和起摆方向，不能整齐同步。此要求覆盖历史单锤草案。

最新旋转台形状：用户要求把圆盘改成十字，等待臂端对准入口后登台、转动中稳住球、到另一侧赛道再离开。十字四角必须真实空缺；旧闭合圆盘/圆柱承托和原盘面挡杆方案撤下，设计与碰撞按文末十字台要求同步。

最新接驳修正：用户指出当前十字臂与入口/出口没有对齐。撤销此前入口/出口各偏移 .9 米的设计，两端接驳口中心与对齐时的臂端中心同线，保留必要转动间隙。清楚自然的接口优先，不靠横向错位强迫玩家等待。

## 目标与已确认边界

用户要求给建模和关卡设计分派工作，做另一个难度更高、障碍更多的关卡，可以参考《男生女生向前冲》，并补充三个核心方向：大转盘、相邻板块一上一下的升降路，以及需要加速和惯性才能通过的上坡。以这三个方向作为第二关核心，借鉴连续水上闯关、观察时机、失误落水、分段递进和冲线节奏，制作本项目自己的路线与造型。本轮未检索具体节目或声称复刻某期真实赛道。

第二关暂定中文名“水上冲关”，稳定关卡 ID/英文文件名 `water-rush`。最终目标是在本地关卡页可以选择并真实通关的独立第二关；首轮准备稿不是最终可玩交付。第一关保留，当前 UI、金属球滚动感和平端锤/短栏验收继续，不以新关卡替换或取消未完成事项。

- 以第一关当前模型为直接美术基准：同样的象牙台面、倒角、分层底座、支撑脚、固定件、橙色护栏和警示，已有水池环境继续复用。用户最新明确“建模还是和第一关一样，只是加障碍物”，不另做水上闯关主题外观、重新设计池体或用主题分区重配整关色彩。新转盘、升降板、坡道也应像同一套轨道零件。
- 沿用当前移动、刹车、镜头、弹珠直径/质量/摩擦/速度/重力；难度来自路线、机关数量和时机组合，不通过削弱操控或无提示隐形障碍增加难度。
- 现有实体没有跳跃能力，不能设计必须按跳跃键才能通过的关卡。转盘、Y 向升降、多实例相位及倾斜坡面均为本次新需求所需能力，由代码实现后才接入，不能把它们写成已支持，也不需要借此开发编辑器。
- 新关卡允许有自己的正式 `.blend`、GLB 和配置，这是独立内容，不是给第一关重复另存版本。新文件首次建立后直接维护同一份，不创建 v1/v2/candidate 副本或备份；不覆盖第一关文件，不删除历史资源。
- 当前入口保留用户设置的 127.0.0.1:5177；本地版本，无云端、账号、多人、关卡编辑器或排行榜扩展。

## 推荐体验与首版规模

首版按六段递进：第一段三把错速错相的平端大锤、一座大转盘、四块交替升降板、一个横移平台和一段惯性坡，辅以短距离限宽/转弯。三锤按用户最新要求增加，其他核心机关保留。建议三处安全检查点承接转盘、升降路和冲坡前助跑区；实际位置由关卡验证。

1. **入口三锤阵**：起点给观察和制动空间，之后连续通过三把大锤，分别使用不同速度和相位，起摆方向至少一左一右；三把之间留可以停稳观察下一把的路段。三组支架跨赛道，中位吊锤居中，左右包络对称；难度来自分次读节奏与择时通过。
2. **十字旋转台**：中心与四条臂组成十字，绕竖直轴旋转，四个空角真实可掉落。等臂端与入口对齐后登台，在旋转中控球留在臂上，等待到出口的接驳窗口再离开；原圆盘及其挡杆撤下。模型与十字轮廓碰撞一致，不能隐藏一整块圆柱或方盒承托空角，也不能只转贴图或把球强绑到台上。
3. **交替升降路**：首版四块相邻板，奇偶两组错相升降，呈一块上一块下。玩家在相邻台面高度接近时通过，并有可等待的固定岛；不需要主动跳跃键。板上弹珠的承托、升降接触与接缝必须真实验证，不用传送粘球或瞬移补通行。
4. **独木桥接横移缺口**：窄桥收成约一颗球宽的细梁，考验居中控线，再在固定观察岛判断横移窗口。沿用现有轨道材质和机关，保留前后缓冲，不把升降末端直接接成无观察空间的必死组合。
5. **S 弯保速与惯性坡**：弯前助跑、连续左右 S 弯、出弯直接平顺接坡和完整坡顶落脚区。用户要求通过 S 弯时保持速度，才能满速冲坡；路线选择和制动时机必须影响出弯余速。不在 S 弯后再放长直道让低速也能重新加满。原有力、重力、惯性和真实坡面决定能否登顶，不设置隐藏速度门槛、自动弹射或人为拒绝通行。检查点放在弯前助跑区之前，不能复活在坡中。坡脚/坡顶无台阶卡球。
6. **坡顶收束冲线**：坡顶给制动与短距离控线空间，再进入清楚的终点；避免冲坡成功后必定因惯性撞出而失去成就感。

关卡需为三种新核心机关提供参数候选与依据：转盘直径/角速度/出口、升降高度范围/周期/相位/接驳窗口、坡角/高度/长度/助跑距离。坡度和高度要结合现有最大水平速度7、重力16及控制施力核算，但不能只用理想能量守恒推定真实摩擦和动力下的可通行性。建议熟悉后的整体通关目标约60–100秒，仅作设计长度目标；真实试玩前不定死奖牌。

## 当前能力缺口（已核对源码）

`LevelConfig` 当前只有一个 `pendulum`、一个 `platform`，无法通过复制数组项获得多机关。`runtime.ts`、`App.vue`、`state.ts` 直接引用 initial-gravity，Lobby/关卡入口文案也固定第一关。存档按规则版本分桶，尚不按关卡隔离，增加 TS 文件并不会自动成为可选第二关。

第二关所需代码能力为：独立关卡选择与完整卸载/加载；旋转盘及同动障碍、Y向升降平台多实例及独立相位、带真实倾角的坡面代理；按关卡加规则版本区分成绩；必要元数据和进度适配。先复用已有平端锤/横移平台资源与生命周期，限定新增能力服务上述三种核心体验，不扩展推板/翻板/浮力/跳跃或通用关卡编辑器。

## LEVEL-02-NEW · 关卡设计与配置（立即开始）

**职责文件**：`docs/levels/water-rush.md`、必要的独立布局数据 `docs/levels/water-rush-layout.json`，以及代码协议就绪后的 `src/levels/water-rush.ts`。不编辑第一关、Vue、运行时或公共协议。

1. 立即按六段方向完成路线草图与实际尺度布局；交付每段目的、通行方向、路宽/转角、落脚岛、检查点与重生位置、机关中心/扫掠范围、相位关系和可等待位置。按球直径 .85 米核对，不把静态几何无相交当成可通关证明。
2. 在草图中明确单锤、转盘、四升降板、横移台和坡面与各区水面/支架/导轨的关系。完整考虑转动/升降的扫掠空间、台面高差及坡脚/坡顶接缝。重生点必须完整支撑弹珠且远离机关扫过区；检查点圆环落在实际台面内，护栏到转角即止，不重新制造第一关暴露过的穿模。
3. 将“现有字段可表达”和“需要代码多实例/相位/选择支持”区分。未获实际支持前只写设计数据，不往已编译配置填造假的新字段，不为试玩覆盖 initial-gravity。
4. 尽早发稳定的静态布局/碰撞包络给美术，允许其开始第二关建模；当前平端锤和横移台可复用，新转盘/升降板/坡面必须先取得代码的原点、轴向与碰撞协议再定稿。代码交付后编写第二关正式配置，冻结一次可联调版本并记录哈希，后续直接更新同一份。
5. 交付真实路线、卡点记录表、难度递进与奖牌校准方案。每个机关至少有安全通行窗口，连续机关之间可恢复观察。冲坡必须设计“低速起步/充分助跑”对照试验；转盘与升降板必须验证实际带球或承托，不把几何/动画当作功能完成。失败时优先调本关机关参数/布局，保留玩家手感。

**本次第一交付**：可供美术执行的草图、尺寸和静态布局，以及交给代码的多实例需求清单；完成后继续衔接最终配置，不把准备稿当成全部任务完成。

## ART-02-NEW · 第二关模型与视觉（立即开始）

**职责文件**：`art/water-rush.blend`、`public/models/water-rush-track.glb`、`docs/art/water-rush-delivery.md`，必要的同目录制作工具。锤头/柄和平台优先复用当前正式资产，不复制一套同内容文件。

1. 先读取用户关于原文件修改、不备份、护栏端点和平端锤头的最新要求，并直接对照第一关当前 .blend 的台面、倒角、底座、支撑、护栏和材质。第二关沿用同一套建模语言，只按新布局增加大转盘、升降板、坡道等必要结构。仅添加机关识别必需的平面方向/相位提示，延续已有标识语义；取消此前为了“水上闯关分区”额外做主题包装的方向，不重做池水、不把危险边缘画成可挡球护栏。
2. 可先搭建第二关自己的可编辑分段轨道/落脚岛、大转盘外观、升降板和坡道结构，核对第一关可复用资产；以 LEVEL 尺寸进行场景排布。旋转/升降零件必须可独立挂载和实例化，坡道真实面与代理同角度；静态布局未冻结时保留可编辑模块，不提前烘焙不可调整的整关文件。新动态资产如有需要固定使用 `water-rush-turntable.glb`、`water-rush-lift.glb`，导出原点等按代码交付协议确定，不靠模型名发明玩法。
3. 使用真实圆角连接与合适端帽，护栏不能超出转角；检查点和机关扫掠区预留空间，不烘焙重复的运行时圆环、警示线或水面。
4. 静态模型只表达冻结代理对应的可行走面，所有“机关更多”的真实行为交给运行时，不把假机关当作已实现。水池仍由配置生成，支架/支撑不得进入运动平台或锤头全周期扫掠包络。
5. 完成后交付同一正式 `.blend`/GLB、关卡布局哈希、单位/原点/包围盒、三角面/材质/字节与场景预览；不得添加相机、灯光、外部贴图依赖或玩法脚本。后续修订直接修改第二关这份文件，不新建版本或备份。

**本次第一交付**：明确风格与可编辑分段场景，可独立于代码准备；取得关卡布局后完成正式静态模型，继续配合代码游戏画面验收。

## CODE-LEVELS · 当前最高优先级：实际接入第二关

用户质疑“为什么还在第一关”，统筹已取消将第一关收尾作为第二关前置的安排。现在先打通真实第二关入口、选择数据和生命周期，再完成最新三锤/十字/升降/坡面实际行为与完整通关。第一关 UI/钢珠/平端已完成修改保留，尚未完成的独立视觉验证列待办，不继续调参或重复通关占用前置时间；共享代码改动仍需最小必要回归和存档保护。

1. 在现有代码职责文件中定义最小本地关卡目录/选择方式，能从关卡页进入第一或第二关；显示各自名称、状态、检查点数量和成绩。未可玩前第二关如出现在列表需标明准备中，不让按钮进入不存在的运行时。
2. 实现第二关实际使用的三类能力：竖轴十字旋转台的匹配复合承托碰撞、Y 向升降平台及独立相位、静态斜坡方向与碰撞。保留已有锤/横移行为、第一关单实例和原参数。原生运动学代理的转动/平移必须正确传递接触，必要的配置轴向/旋转由明确字段表达；不通过瞬移、按位置强绑弹珠或改其摩擦掩盖问题。不改引擎、不构建通用编辑器。
3. 运行时接收明确选中关卡数据，切换时完整释放旧模型/实体/输入和采样，避免两关同时运动、旧回调写入当前状态或加载竞态；HUD/检查点/进度读取同一份选中配置。若采用现有沿 Z 分段进度，应与 LEVEL 路线匹配，不宣称是精确路径进度。
4. 成绩必须同时区分关卡和规则版本；所有历史 classic/open-hammer/flat-hammer 记录归第一关并保留，设置保持共享。不能让第二关最快成绩覆盖第一关，重复迁移/切换不丢桶、不清数据。
5. 模型多个实例按同一已确认米制协议复用，使用独立实例和正确生命周期。代码协议先写实际字段、资源挂载和就绪通知，再由关卡填数据。
6. 两关均需可启动、暂停、重开、掉落重生、结算并刷新保存。第二关至少完成一次真实全路线，记录分段失败与等待，检查转盘接触作用、相邻升降接驳、横移露空和坡面低速/助跑对照；第一关只做必要回归。多关卡成绩、切换清理、新机关几何/姿态、多实例相位与失败回退需要对应的有效测试。
7. 统一 npm test / npm run build，实际服务使用5177且保留用户端口改动。运行验证由代码独占，使用隔离存储；向关卡、美术提供关键游戏画面以校准难度和外观。

## 协调与完成标准

产品：`01a08e33-7e18-7060-8cb7-a9c21af2a1f0`；关卡：`01a08e31-6397-7ec3-990c-4714c4a34fd7`；美术：`01a08e30-d06f-7331-9d87-66fa057ba005`；代码：`01a08e20-1eb7-7c03-9b62-2eb74ed722d9`。

关卡先交草图/静态尺寸 → 美术并行做分段模型 → 代码交实际多实例/选择/存档协议 → 关卡正式配置冻结 → 美术配套导出 → 代码接入试玩 → 产品与用户验收。各自只改职责文件，可直接互发交接消息，产品独占本文件；一旦首份布局/协议就绪即交接，不必等整份报告全部写完。

最终验收：第一关仍可选且保留数据；第二关为独立可玩路线，更多障碍和更高时机/控线要求在实玩中成立，所有机关有合理通行窗口，无新增穿模、隐形墙、错误重生或跨关串成绩。未经真实试玩不宣称难度已校准。第一关 UI/钢珠待办继续由原任务收口。

当前状态：LEVEL-02-NEW、ART-02-NEW 继续第二关设计/建模；用户明确只让代码优先 GitHub 推送、CI/CD 和服务器部署，CODE-LEVELS 暂缓实现，当前 UI/钢珠剩余代码也保留待续。统筹最初误发给关卡/美术的暂停通知已取消，两方不必等待部署才继续独立工作。第二关尚未实现可选或可玩；具体第一关当前部署安排见 round-03.md 文末。

用户最新美术收窄已同步两方：第二关使用第一关同款轨道与支撑系统，只增加新障碍及其必要连接，不另造主题。当前 Blender 预览是可编辑排布阶段，不能把缺少第一关底座、倒角和支撑细节的白模当最终效果。大转盘、交替升降、惯性坡的玩法目标和独立路线继续，不因视觉风格统一而取消。保留现有 water-rush 文件名，直接在这份第二关文件中继续做，不另建主题版本或备份。

## 路线修正任务：机关之间弯绕过渡

用户最新截图中，转盘之后的升降、窄桥、横移、助跑坡基本共用同一长轴，缺少行进方向变化。用户明确要求“不要这样一列排开，弯弯绕绕一下，比如在每个障碍物之间的过渡”。这属于本次已授权路线调整，不需要重复确认。

**关卡立即执行**：在现有 `water-rush.md` 和 `water-rush-layout.json` 中重排静态过渡、机关位置及检查点，保留六段体验与同款模型风格。

- 整体采用紧凑 S 形或 U 形折返布局，至少有三处明确的方向改变；不能仅把整条直线在场景里旋转后当作弯绕。
- 在单锤→转盘、转盘→升降、升降→横移、横移→冲坡之间安排短直道、L 形转弯或 U 形回转的组合，交替左右转向。过渡兼有减速、重新瞄准下一机关和短暂观察作用，避免全是装饰性长绕路。
- 四块升降板在自身挑战段内仍可直排，平台接驳局部结构优先保留已核对关系；通过整个机关段移位和段间连接实现路线变化，不随意破坏升降相位或改变核心挑战。
- 冲坡之前必须保留完整直线助跑长度，检查点位于助跑区之前。可以先转弯再进入助跑，不把急弯放在坡脚前而消除惯性冲坡机会。
- 用足够宽的落脚岛承接急弯和安全检查点，圆环必须完整落在台面内。相邻折返道保持间距，运动包络、支架和高坡不互相穿插，不能让玩家轻易跨到后段绕过必要检查点，也不能让镜头中的上层轨道遮住当前路段。
- 优先轴对齐折线与清楚接合的角台，沿用第一关倒角语言；不制作显示有弧道、碰撞却缺路或越界的假转弯。护栏仍到实际转角止，不重新加出多余短端。
- 第一交付为一张能看清完整行进路线的俯视草图/布局预览及更新坐标，先发给美术再完成细节说明；用户关心的是空间布局，不能只交 Mermaid 顺序图而看不出仍为长条。

**美术配套执行**：暂停以旧单轴长条作为最终排布继续制作，保留当前可编辑模块，继续同款轨道/支撑等独立细节；取得新坐标后在 `art/water-rush.blend` 原位移动重接，并更新现有预览/交付说明。不得另建“弯绕版”文件或备份，不自行编造一套与关卡不同的坐标；尽早提供完整俯视与斜视预览。

**验收**：全关俯视可一眼看出不同朝向的机关段与连接弯道，整体不再是一条细长单列；新转角、重生点、坡前助跑、机关扫掠和路线唯一性有几何依据。89.7 米等旧布局长度/坐标及其静态检查只属于调整前设计，更新后重新计算；真实可玩性仍等代码能力就绪再测。代码继续 Git/CI/CD，不因这次布局修改恢复功能开发。

折返首份交付已到：统筹已查看 [完整俯视图](../levels/water-rush-top-view.png)，并实算布局 JSON SHA-256 为 `71615a27dc43d368a20f1974786d11a567f2ddd3e029ad93dea177358f194fd3`。当前排布约45.3×33.52米、六次转向，四升降板沿+Z、窄桥/横移段沿-Z、最后助跑与坡道沿-X，12米助跑保留；俯视可清楚看出折返，不再单列。该图是设计图，不是游戏截图。美术以当前JSON中的 topStart/topEnd/坡体中心和旋转为准，不用此前口头 X32 向北坡的旧方向。此版锤子 anchor 仍偏Z一侧，下面新门架修正尚待关卡补齐，不能因折返已交付就把锤结构也验收完成。

## 大锤结构修正：跨道门架与居中吊点

用户指出当前大锤只挂在一侧、摆动路线不合理，明确要求左右各一根方柱，顶部横梁中间连接锤柄，使锤头静止位于赛道中央再左右摆动。按当前截图对应的第二关处理，第一关资源与已发布代码不自动改动。

统筹读取弯绕排布进行中的设计数据：此刻 hammer.bodyCenter 为 `[-12,4.3,18]`，anchor 为 `[-12,7.9,16.4]`，两柱在 X=-12.8/-11.2、Z=16.4，同处行进方向 +X 的一侧。说明头中心虽然在路中，吊点与支架仍继承了旧偏置。坐标仅用于定位当前问题，后续以关卡统一更新的当前 JSON 为准。

**LEVEL 执行**：

- 用行进方向定义“左右”，支架跨越赛道横向，而不是两柱沿行进方向排在同一边。当前锤段沿 +X，则两柱应在中心 Z 两侧，横梁主要沿 Z；若新布局改变段朝向，整套支架、吊点和摆动轴一致变换。
- 吊点的水平坐标与锤头中位完全一致。以单锤旧位置为例，anchor 应在 `[-12,7.9,18]`，中位头中心 `[-12,4.3,18]`，锤柄垂直；三锤分别套用各自中心。建模静止中位居中，正式开局按各自相位呈现不同姿态；暂停冻结当时姿态，重开恢复各自初始相位，不强制三把同时回中。
- 左右幅度以中心线对称，保留当前合理的频率和击球空间。设计优先采用固定杆长圆弧（当前中位杆长约3.6米），锤头从中位向两侧抬升，不能用缩放杆长和侧偏吊点表现非对称“甩出去”。将圆弧/轴向写明为第二关接入要求；运行时尚未具备的运动方式不写成已实现。
- 两方柱与横梁应避开锤头、连接套和锤柄整个摆动包络，不能只检查中位。明确柱内净距与两端极值的余量，必要时把支撑放在行驶面外并给柱脚可信基础；不能靠扩宽通行台面或隐形移走碰撞解决支架穿模。
- 更新同一 `water-rush-layout.json` 的支架/吊点/中位/左右极值/扫掠范围，与正在进行的弯绕布局一起交美术。平端锤头和细柄造型、转盘/升降/冲坡目标不变。

**ART 执行**：

- 在当前 `water-rush.blend` 里改跨道门架，沿用第一关深灰方柱、横梁、固定件语言。锤柄从横梁中央吊点连接，提供中位正视、左极值、右极值和整体俯视预览，四张图可共用现有预览输出位置，不新建版本或备份。
- 静态支架模型与动态锤头/柄分件继续遵守已有协议，锤资产可复用；不把锤头或柄烘焙成静态场景。默认中位要能看出左右结构对称，不能靠临时转视角掩盖偏置。
- 当前 GUI 的 *water-rush.blend 有未保存标记，美术已在其任务向用户核对是否存在几何/材质手改；不重复询问整项修正授权，也不在未确认保留范围时从磁盘重建覆盖当前场景。可继续布局交接/只读净隙检查，获得澄清后在当前内容上定向修改，保留无关用户编辑。

**代码边界**：本次先完成第二关设计数据和模型结构，代码功能开发仍按用户部署优先安排暂停。居中固定杆长圆弧及相应实体/模型/碰撞姿态列入第二关恢复接入时的明确事项；不触碰已发布第一关或自动推送新版本。当前模型预览通过不等于游戏动态验收。

**验收**：从玩家来向看到两柱分立左右、横梁跨道、锤头中位居中垂下；左右极值关于中心对称，柄头连续且不穿柱/梁/路面。柱脚、通路和机关扫掠关系清楚，第一关、其他第二关机关和用户未保存编辑保持。当前任务为结构修正进行中。

## 第一段追加：三把大锤错向错速

用户明确“一把不够，要三个，但是不要一致摇摆，方向和速度要错开”。保留跨赛道左右摆动的轴向，通过反向起摆和独立相位错开方向，不擅自把其中一把变成沿行进方向纵撞的另一类机关。

**LEVEL**：在原 JSON 和说明中设计三个锤实例，逐一给中心、正上方吊点、左右两柱、杆长、摆幅、角速度、相位与观察点。速度采用可辨的慢/中/快，起摆至少一左一右，第三把额外错相；数值为需试玩的候选，不称已平衡。锤头/柄仍同款资产。起点观察段保留，锤间扫掠之外留完整弹珠可停稳的空间；必要时局部延长第一段并接回弯道，不恢复整关长条，不挤入邻道。更新原俯视图，尽早把三组坐标交美术。

至少用覆盖各锤两个周期的设计采样或动画显示错相关系，检查关键共同相位和逐把通行机会。不保证永不同时挡路，但必须存在观察/等待区，不能长时间无解或重生即连续被击。默认静止几何居中与运行中错相位置分开。当前 JSON 是设计数据，三实例不意味着运行时单 pendulum 已支持；独立实例、速度、相位列入代码恢复接入事项。

**ART**：按关卡坐标在当前第二关文件内布置三组跨道门架，锤头/柄复用同一正式资产实例，不导三份同内容 GLB。各柄连各自横梁中点，检查三组极值包络及其与支架/邻道的间距；提供整体三锤段与错相姿态预览。静态 GLB 只含固定架，动态头/柄不烘焙。用户未保存编辑核对、原文件修改、不新版本或备份继续。

**验收与边界**：第一段有三把，速度与当下运动方向明显错开，每把能独立观察择时通过；玩家控制参数不变。此阶段先交关卡和模型证据，代码仍暂停，真实三锤运动、碰撞和通关待恢复接入后验证。

单组门架结构交接已到：LEVEL 回传并由统筹核对设计文件哈希 `71ade229ec599f89df73c48448b7372e47ae24383b961ff21305b8e0331b7302`，吊点 `[-12,7.9,18]`、静止头中心 `[-12,4.3,18]`，固定杆长3.6，最大摆角约27.28°；头沿居中圆弧运动、头柄旋转一致，不再沿用旧侧偏/伸缩公式。左右柱、横梁、脚板已给出尺寸及净隙，`motion.runtimeSupported=false` 准确保留能力边界。现有 `water-rush-hammer-poses.png` 为单组结构设计示意，尚非三组运动或模型验收；三锤应各自套用此居中模板并独立分配位置、速度、相位和观察间隔，不能把这次单组交接当成完成最新数量要求。

LEVEL 后续已交三锤设计（布局哈希 `626307ecf24f077532570286358cc183a5cfbe09d1152fa68d3d8d88e5e4d25e`）：三中心 X=-24/-18/-12，频率1.1/1.5/1.9、相位0/π/π/2，固定杆长3.6，观察间隔3.85米；24秒采样分别覆盖约4.20/5.73/7.26周期。折返六次与12米助跑保留，旧单锤坐标已被替代。这是关卡设计阶段回传，不是游戏通关；下面十字台新需求还需在该基础上继续修改。

## 旋转台改为十字：登台、随转控球、对岸离台

用户原意为“球需要在十字的一条边转到赛道的时候上去，然后不被十字转动滚下去，然后转到另一边赛道再去”。先在现有第二关设计与模型实施，不恢复暂停中的代码开发。

**LEVEL**：

- 将闭合圆盘定义替换为平面十字：中心方台加四条等高臂。优先保留原盘心、台面高度及约8米的整体跨度，臂宽从约1.6–1.8米候选起算；这些是设计起点，需与球直径.85、倒角和入口短舌共同核验。局部必要调整要记录，保持三锤、折返、升降和助跑坡数据。
- 四个角的区域必须没有可见台面和承托碰撞；原 coMovingBar/挡杆不继续添加。核心挑战来自臂宽、旋转和上下台时机，不额外叠旧扫杆，也不设计成大中心平台可以一直站着等所有机关自动完成。
- 设计匹配十字的复合几何，优先中心加四臂的非重叠盒体分区；代码恢复后决定同一运动学复合体或同步部件的最小实现。不能用覆盖整个包围盒的单盒、整圆柱或填满空角的凸包替代；中心拼接需无明显接触台阶、重复面或抖动。
- 入口/出口短舌按臂端对齐时核对真正接缝，记录可登台、可离台的角度范围与时间窗口。旋转扫掠使用臂端外角的实际半径，不能仍拿旧圆盘半径代表全部外轮廓；全周期不得扫进两岸、支撑或邻道。
- 首版可参考原角速度.55rad/s，但必须记录臂端速度、控球空间与转乘机会。以“入口等待→臂端对齐登台→控球随转→出口对齐离台”为主路线，核对相反岸位和交叉中心是否会形成随便直冲就跳过全部转乘的捷径；不靠隐藏判定强迫等待。
- 保留四臂的真正开放边缘，不增全包护栏让球不会掉；岸上有固定等待区。之后实测要验证人在臂上能通过原控制纠偏、空角会落水，不强绑或传送球。
- 更新同一 layout JSON、俯视图与说明，明确旧 `closed Y cylinder`、圆盘挡杆、圆边接缝计算和原 sweep 已过时。新的复合旋转碰撞/接触仍为尚未实现的代码需求，不填写运行 TS 宣称已有。

**ART**：在当前 `water-rush.blend` 原位将圆台改十字，保留第一关同款台面倒角、分层底座和结构材质。移除填满四角的圆盘上下层及旧挡杆，只保留与中心支点对应的必要轴承/支架；动态模型仍用原固定 `water-rush-turntable.glb` / `TurntableVisual`、原点旋转中心，避免新版本/备份。按 LEVEL 的四臂及接驳尺寸导出，提供对齐入口、旋转45度空角、对齐出口的预览，清楚显示真实镂空和无穿插。未保存用户编辑继续按既定保留范围核对。

**验收**：外形清楚为十字；站在原圆盘四角位置没有隐形承托，错过臂端会落水；原控制下可择机上臂、在旋转中保持位置，再从出口离开。等待岛、臂端和支撑全周期无穿模，四臂无缝连接，模型/实际碰撞匹配。当前先验设计及模型，代码能力与游戏实测待恢复接入后完成。

### 十字入口/出口必须正对臂端

用户看到预览后指出“没对齐吧”。统筹核对设计数据：十字盘心 X=-6，入口 mouthCenter X=-6.9、出口 X=-5.1，确实有刻意错位；不是截图视角造成。该偏移用于抑制直冲的设计撤销，不辩解为完成了用户所要的对齐。

- LEVEL 将两个接驳口水平中心统一到 X=-6；按当前盘心 `[-6,3.15,10]`，入口/出口口沿候选分别 `[-6,3.4,14.3]`、`[-6,3.4,5.7]`。两端台面高度与十字均为 Y=3.4，正常对齐姿态为臂端沿±Z、yaw=0（同臂到对岸为π）。左右有意错位字段 offsetXEach 改为0，原参考角/角窗/名义接缝计算全部重算，不只改预览。
- 修正范围为接驳短舌、其显示底座和相关边线/连接数据；岸上等待岛、十字尺寸/中心、三锤、升降和助跑坡保持，除几何核对明确需要的最小调整。臂宽1.8与短舌宽1.6可以是居中收窄，边缘应对称，不要求为了等宽改整个十字。
- 保留臂端到固定口沿的转动缝，按当前值在正对时约 .3 米；必须重查球真实能跨过以及臂端外角旋转不扫进固定岸，不能直接把缝合死制造穿模。模型与碰撞同改，入口/出口方向、中心线和高程在俯视/正视清楚匹配。
- ART 在原文件中按 LEVEL 新口沿位置修正短舌及其底座，十字取 yaw=0 做对齐预览；再提供斜转姿态证明间隙。不要仅转相机、旋转整台或移动显示而留下旧碰撞/旧数据。
- CODE 已获用户恢复许可，接入时只采用修订后设计；两岸中心线对齐后的登台、在臂上控球及离台由实际物理验证。若熟练玩家能通过中心更快过关，可以记录为技巧，不能再用错位端口或隐藏等待规则限制。

本项优先纠正用户指出的可见接口，继续保留真正十字空角和旋转承托目标；原“反向错位入口/出口”的数据和图不作为最终验收。

## 正式配置与入口接入进展

统筹实算 `src/levels/water-rush.ts` 哈希 `c1e3a44092c0191f3e8ee42b6f97c35fc3e3ac0f7746b4de9bf500c9bb12373d`，规则 standard，三锤、十字五盒、四升降、向-X坡、三检查点和正式资源引用已填写。LEVEL 回传类型检查与24/24测试通过；此为配置/自动检查结果，尚非通关证明。

统筹已读取 `src/game/levels.ts`，确认导入 waterRush 并注册为02“水上冲关”；Lobby 已遍历目录生成选择按钮，state 的 selectLevel 切换关卡、清理局内状态并切成绩归属。下一步直接针对此实际入口做加载、实体/碰撞、多机关、切换存档和全路线验证，不再等待图文补记或重复注册任务。当前本地入口为 http://127.0.0.1:5177/#/levels，线上镜像是否含此版本取决于后续提交发布，不能将本地未发布内容写成线上已可玩。

CODE 已实际从该入口选择第二关并 ready，游戏开局为 `docs/integration/level-02-evidence/02-start.png`，选中页为 `01-selected.png`；统筹查看开局画面，HUD 显示02水上冲关及0/3检查点。运行采集回传仅1个Canvas，26个静态碰撞、3个平端运动学锤、4个升降盒、5个十字承托盒、1个横移盒和玩家，6份容器资源被多实例复用。此为本地当前配置构建/实际加载证据，未包含在已发布15f6f68镜像中；CODE 正在做不瞬移、不改玩家速度的全路线实测。

## 用户实玩调难：四段挑战明显加强

用户反馈三锤太慢、十字不容易将球带偏掉落、升降高差小且慢、最终上坡没有难度。此为明确调难授权，不能用“首版已能通关”结束工作，也不能只改标签为困难模式。保留第一关、第二关折返路线和原玩家手感，集中修改四段机关的参数与必要几何。

### 首次通过仅作为对照

CODE 回传原 standard 版88.1256秒/1掉落/3CP，过程记录位于 `docs/integration/level-02-evidence/route-first-complete.json`（1039帧、52接触）、`04-finished.png`。已过三锤、十字转乘、四升降、横移和坡。此时坡低速对照初速为.84m/s，不是严格静止或<=.5m/s；不拿它证明“无助跑上不去”。该原难度成绩保留，新难度不混入旧standard排名。

### 四段第一轮参数与几何目标

1. **三锤更快**：原角速度1.1/1.5/1.9 rad/s，第一轮候选1.8/2.3/2.8，周期约3.49/2.73/2.24秒；原相位0/π/π/2和不同起摆方向继续。保持固定杆长、跨道居中、扫幅和锤间观察区。观察窗口明显缩短且锤击更有压力，但不把速度拉到高帧率才能判定或穿透碰撞的程度。
2. **十字更考验控球**：先核对真实转动接触有效；原.55 rad/s、臂/中心宽1.8，第一轮候选.85 rad/s、宽1.4，保持8米跨度、同高对齐口和真实空角。中心与四臂碰撞和所有模型层一起收窄，不留旧宽代理。正对入口/出口仍居中，臂端外角扫掠与.3米接缝重新核验。保留可主动纠偏和择时离台的空间；不能伪造离心推力、把球强绑到台上或再加隐藏等待门槛。
3. **升降幅度和速度提高**：原振幅.45、角速度.7，第一轮候选振幅.9米、角速度1.1rad/s；总升降行程1.8米，相邻最大高差1.8米，周期约5.71秒。四块继续0/π交替相位。板体、伸缩杆/套筒及支撑全范围复核，避免低位插进基座、高位脱开或出现新台阶。玩家可以在板上等下一次接近同高的时机，不需要跳跃键；过台窗口必须用真实球体接触检验。
4. **真正需要助跑的坡**：先做旧坡严格低速/充分助跑短对照，再以主坡55°、主坡斜长约5米作为第一轮候选，前后缓坡仍优先20°并精确接合，保留12米助跑。该候选理论主坡重力分量大于持续输入沿坡分量，惯性消耗更大；但摩擦、阻尼、滚动能量及坡脚接触不能用理想公式替代。目标为坡脚静止/<=.5m/s持续向上不能轻松登顶，充分助跑接近原正常上限可以登顶，坡顶仍可制动。坡顶高度、落脚台/终点/圆环和支撑按新坡端点同步，不能只改碰撞或用隐形速度门槛。

以上数值已获统筹作为第一轮试调方向，不是硬性最终参数。LEVEL/CODE可依据真实失败原因做同一范围内必要的小幅调整，记录前后值与理由；若仍过易则提高对应局部挑战，若完全不可通关则放宽该段窗口或坡参数，不改玩家摩擦、质量、控制力、刹车、限速或镜头，不把试调扩大到其他关卡。

### 分工与交付顺序

- CODE先建立十字实际切向作用和坡面低速/助跑对照，给LEVEL可复查结果；准备第二关独立新难度规则（建议 challenge）并保留既有standard及第一关全部成绩。此处需要规则隔离，不新建模型版本或玩法页面。能力/归属就绪后通知LEVEL启用。
- LEVEL负责原 `water-rush.ts` 和设计JSON同次更新参数、必要几何、终点与规则标识，数据归属不转交代码代改。先给ART十字宽度、五盒分区、新升降扫掠和坡面精确端点/姿态；重新审计保护字段和检查点安全区，保留其他已确认机关/路线。第二关池显示覆盖问题可在本人范围一并修正，不变真实碰撞。
- ART按新数据在当前water-rush.blend与同名GLB原位调整十字、升降结构和坡面/支撑，保留第一关同款材质、端口对齐和三锤结构；仅锤速变化无需重新导锤资源。新旧模型几何有差异时明确暂态，不拿旧模型/旧通关验新参数，不创建副本或备份。
- CODE在数据/模型配套后真实验证四段：三锤错速错相且可逐把过；十字比旧版更需纠偏但能完整转乘，空角掉落真实；升降可见更大/更快且能逐板过；坡脚严格低速失败、助跑成功的对照成立。随后完成一次新难度全路线，记录分段等待/掉落、版本归属、重生和刷新成绩，运行统一测试/构建。

验收以用户可感知的挑战增加及可重复真实通行为准，不能只交一组更大数字或只凭总用时变长判断。没有证明接触工作前不盲目加转速；没有严格坡对照前不宣称惯性门槛达标。当前用户反馈已分派，原低难度的其他重复全路线验证不再作为前置。

### 最后一段追加 S 弯：保住出弯速度才能冲坡

用户提出最后上坡前加 S 弯，需保持速度过弯才能满速上坡。统筹按“带着足够水平速度出弯再冲坡”理解，作为本轮调难中的最后一段更新；其他三锤/十字/升降调难继续，不被替换。

- 原12米全直线助跑不再是硬约束，改成“检查点三固定岛 → 弯前助跑 → 连续反向 S 弯 → 很短的切向衔接 → 上坡”。允许保留弯前必要蓄速长度，但出弯后不能再留足以从低速轻松加满的长直路。S 弯出口方向与坡面行进方向平顺对接。
- S 弯必须是可行走台面和匹配碰撞共同形成的路线，不是在宽直路上画 S 形线。用足够曲率半径、合适路宽和连续接合让原控制有保速过弯的可能，不能做必须急停的直角迷宫再要求满速出弯。玩家可以通过走线、提前转向和少刹车保速；碰撞、倒角和边缘提示清楚，不能靠隐形导向或把球强绑中心线。
- LEVEL 给出弯前助跑长度、S 中心线/弯向/最小半径、台面与代理分段、出弯切线、坡脚接点及坡顶/终点数据。优先使用已支持的静态旋转图元构造平顺通路；若这些图元无法可靠表达弯道边界，先向 CODE 交最小几何需求，不只改 GLB 留下不匹配碰撞。
- 继续保持第一关同款轨道/支撑/护栏语言、原位改第二关文件。新 S 弯与邻近折返路、高坡/支撑、池沿留空间，不能跨道抄近路直接绕过 S 或产生新遮挡。检查点三保留安全重生意义，必要时移到 S 弯前的稳定助跑区，不能放在出弯后让失败重试绕过保速挑战。
- CODE 先用相同原始物理完成 S 弯可保速的短验证，再结合本轮加陡主坡候选联合校准。记录水平速度 sqrt(vx²+vz²) 的入弯、两次转向、出弯、坡脚和登顶/回滑结果；“满速”按接近原上限的有效余速验证，不承诺每一帧精确等于7，也不写硬编码速度门槛。
- 对照至少包括：合理走线、少刹车过 S 后能够冲上去；弯中明显制动丢速后同样持续向前输入无法轻松登顶；坡脚静止/<=.5m/s起步的原对照继续。不能通过出弯后自动补速、提高球体施力/摩擦或增大速度上限让候选通过。若合理路线也上不去，调整本段曲率/宽度/坡参数，不取消保速目标。
- ART 在 LEVEL 数据就绪后同步更新原静态轨道与支撑，S 弯所有层及坡脚接缝、护栏端部一致；不新建版本或备份。代码、关卡、美术先共享能试的几何，报告补记不做前置。

S 弯与四段调难合并为同一待交付挑战规则，不因为本次追加再多建一个成绩版本。原 standard 成绩保留；新难度数据与模型配套后完成全路线和成绩刷新验证。前文关于“必须保留12米纯直线助跑”的历史条款由本节覆盖，当前先完成数据/碰撞/模型与原手感下的真实对照。

### 窄桥追加：约一颗球宽的独木桥

用户觉得当前窄桥仍宽，要求改得更细，类似独木桥。统筹核对当前 `narrow-bridge` 宽1.6、长8、中心 `[20,3.22,13]`，台面高3.4；玩家直径.85。本次第一轮目标为宽 **.9米** 的细梁（约一颗球宽），长度/高度/中心与两岸岛优先保持。不是改木质主题，继续第一关同款机械轨道材质。

- LEVEL 同步原设计JSON和正式配置，把承托盒宽从1.6改.9，保留长8与厚.36；CP2/入口固定转向岛、出口观察岛及横移机关先不动。新难度归属与正在调的其他机关/S弯一起提交，不另加一轮成绩版本。
- 这段两侧开放、不新增护栏或自动纠偏。前后可以有清楚的接入缓冲，但中间有效长度保持细梁，不通过隐藏宽碰撞、透明托板或低矮墙把它变回宽路。
- 原12段平面警示线按新边界内移并减细，不悬在梁外、不变成有碰撞挡条、不在GLB重复。入桥中心线要与两岸对应，接口同高、无卡球台阶，倒角与上沿尺寸按细梁比例校准。
- ART 在当前第二关静态源文件和同名GLB内收窄台面、底座/橙夹层及相应固定件，支撑对齐；不要只缩上层留下视觉上仍像宽桥的底板，模型与.9米代理对应，不改第一关资源、不新版本或备份。
- CODE 核对精细模型/基础回退均为新宽度，中心线可控通过、偏离边缘会真实落水、CP2重生后可重新对准；用当前键盘与原玩家物理验证明显更窄且可通过，不调整球体大小、摩擦或镜头辅助。若.9米在真实接口处完全不可行，先定位接口/倒角还是操控窗口，再回传最小局部调整，不直接放回1.6米。

本次与三锤提速、十字更快更窄、升降更大更快、S弯保速冲坡合并为当前第二关调难，所有旧standard证据只作对照；配套后完成新挑战全路线，记录各段而非只用总时间判断难度。

美术执行协调：ART 已准备十字1.4、独木桥.9及升降杆/套筒更新，当前因GUI未保存标记暂未覆写资源。统筹要求先只读识别具体差异与来源，优先对当前打开的正确场景做授权范围内定向编辑、保留其他内容，不重载磁盘/全量重建覆盖。未保存星号本身不是具体用户冲突，也不等于缺少整项实施授权；只有确认本次目标对象与用户手改实际重叠才回传精确冲突等待决定。工具无法读取当前场景则说明工具限制，不重复泛泛询问用户。代码可继续基础几何与手机输入验证，最终视觉验收仍要配套最新GLB。

challenge 实际启用：统筹核对当前 `water-rush.ts` 哈希 `bf71d307cc3f75c6002f64153c0bb2a886a6f77fc15ee540a073f4e4416c9098`，规则为challenge，已合并R8/宽2.2/两90度反曲率S弯、36个真实旋转静态盒、弯前5米与弯后.6米连接；坡脚移至 `[-4,3.4,-26]`，55°×5米主坡及坡顶/终点按新位置同步。.9独木桥与其余调难值同时写入，不是旧12米纯直路终稿。新静态/十字/升降暂不引用旧GLB，使用基础外观测试。LEVEL回传类型检查通过，CODE已接续S弯保速/制动丢速短对照；三CP与第一关保持。

### S 弯保速已证实，主坡局部收敛

CODE 完成当前55°/5米短段实际对照，记录 `s-55deg-short-comparison.json`：R8 S弯无掉落，入弯6.733、两次转向6.945/6.915、出弯6.762、坡脚6.189m/s，但主坡最高球心约Y7.088即回滑，未到台面Y8.3508。弯末制动组坡脚2.726、静止组0也未登顶。试验用CP3或坡脚的独立测试出生配置、同一createGame与真实碰撞、原键盘施力；这是短段参数对照，不是假称从正式起点全通关。

依据该实测，S 线形和保速能力暂保留，55°/5米不作为最终坡度。下一候选主坡 **50°、斜长4.25米**：LEVEL 只更新主坡及连带坡顶/终点/支撑精确坐标，ART相应配套；三锤、十字、升降、独木桥和S不因此回退。CODE取得后立即重复保速/制动丢速/静止三组短对照，目标仍为保速可达、明显丢速不可轻松登顶。不得补推力、改球体物理或绕过S。该局部收敛属于先前已授权的按实证调参，不需重复用户确认。

50°/4.25米三组短对照已达到当前目标，确定为本次配套坡值。CODE回传 `s-50deg-short-comparison.json`：良好走线出S6.8759m/s、坡脚6.0672，实际到新高台，球心7.9357且未掉落；弯末制动组出S.0842、坡脚2.6829回滑；坡脚0速组亦回滑。S半径8、出弯连接.6米及原玩家参数保持。这是同一真实运行时的短段验证，不是正式起点的全关完成；旧55°/5米不再作为最终导出值。

LEVEL按50°/4.25米冻结当前JSON/TS及连带终点、栏/支撑；ART按该版本在同名文件内完成模型、交新哈希；CODE待配套恢复资源引用后验证完整challenge、三锤/十字/大升降/.9桥与手机双指和横屏HUD。其他范围不扩大，不因坡短对照通过提前关闭整关或手机验收。

### challenge 精细资源已配套接入

ART已按几何源JSON `d23759cb2f79dc11cc1b7138f12123c3a4aa6fb3394d532c0f1225050b5f1ed4` 完成当前文件内修改。统筹实算源blend `9458eded8a2213cbaa9cbcb53052917fda78b3cb0816fb53bf52f8cff212d7e0`；三个同名GLB为 track `72b738df516f2f2854d3b126bce9342f22eed6e720aeca4bae3fba217433195a`、turntable `9333a0f8277158ade0c72f1089fc2c6586fefc75ab865f9e19bf82c706db1fed`、lift `70cd31683b429b47553d3f534170fc877931c2652de033387b7e9dfaf59d0ff1`，与美术报告一致。63控制体、S台面采样及运动包络净隙检查由ART交付；第一关与模型文件名保留。

LEVEL随后仅恢复静态/十字/四升降visual引用，正式TS哈希为 `c2d65e7344f52b3d779f7d5196293a81447665ee0e7580589d1995740ffeda69`，当前JSON元数据为 `eb9aa5d196407af67d5d50ec9137734522006c371e55f17ba1c57d8c7bb9bd53`，原ART几何源指纹保留。统筹已核对文件；LEVEL回传前后深比较仅引用变化、32/32测试及构建通过。此前基础外观暂态结束，CODE已收到最终配套，可直接做精细模型全challenge和移动端验收，不等待报告排版；资源/短对照通过不等于整关已完成。

## 按用户手绘再收紧：连续回头弯、细梁与更严格冲坡

用户再次指出当前仍太简单，要求S弯按 [手绘参考](round-03-evidence/tight-serpentine-reference.png) 更曲折、独木桥更窄、坡更难。参考是反复上下回摆的蛇形路线，明显超出当前R8、两次90度缓弯。不能再以原三组坡面对照通过作为产品难度已经满足的结论。

### 蛇形按图重做

- 用约四个连续反向、接近180度的回头弯表达图中的反复回摆，保留顺滑切线连接。转弯半径先以约3–4米、路宽约1.6米为设计候选，代替R8/宽2.2；具体尺寸由真实操控可达性与空间排布校准，不只增加贴花或微调现有两弯。
- 正式建模前尽早交同文件俯视轮廓和静态代理数据，能直接与手绘比较出“更紧、更多次回头”。弯前允许蓄速，但最后一个弯之后仅保留必要的短切向衔接，不增加可重新轻松加满的直道。
- 保留中心线清楚、台面/碰撞一致和第一关同款细节；可以用现有静态旋转盒体充分分段形成路线，但不能填满各回头之间空区、残留旧宽缓S台面或造成隐藏捷径。
- 原控制下急弯可要求提前减速和精确走线，不强求每帧满速；好的走线应在最后出弯保留足够冲坡的水平速度，错误走线/过度制动损失应影响登顶。不能改球体力/摩擦/速度上限强行适配。
- 各回头臂之间、支撑与邻道之间留实际空隙，视野不能被高坡挡住。CP3放在这整段之前，失败可重试蛇形，不把重生点移到坡脚绕过核心挑战。

### 独木桥先收至 .5 米

- 第二关桥宽从当前.9米进一步收至首轮 **.5米**，长8米、台面高3.4及中心优先保持。球直径仍.85，允许球两侧自然悬出窄梁，只要真实接触承托成立，不要求整球投影必须被台面包住，也不缩球。
- 桥体、底座/夹层、固定件和碰撞一起收窄，警示线按新边界减细或合并成清楚的少量平面标记，不能挤满整根梁。保留入口/出口固定岛和无护栏开放边缘，不增加隐藏宽托板或自动导向。
- CODE验证居中能通过、偏离真实掉落、CP2可重试，手机微量方向与刹车也要测；不以键盘保持直线一次不掉便判定已经够难。出现接口阻塞先修匹配和接缝，不无依据放回宽桥。

### 坡与新蛇形联合校准

- 当前50°/4.25米和此前55°/5米都是旧R8线形下的对照，不能直接据此锁死新坡。用户要求最终更有挑战，CODE先采样新蛇形合理走线的出弯/坡脚速度，LEVEL据此增加所需惯性余量（坡角/长度/高度局部组合），不是无条件把两项都调到极端。
- 验收需区分较好走线、一般丢速走线、明显制动及坡脚静止：较好走线有可重复登顶路径；明显丢速和静止不能轻松登顶；一般走线不能像旧版几乎随便上去。以真实运动/接触结果和用户体验判断，不用隐藏速度门槛或自动补速。
- 若新紧弯出速下降很多，应先协调弯半径、最后转向与坡参数达到目标，不把无法完成当作更高难度。所有旋转盒体顶面、坡脚/顶台/终点/护栏/支撑同步，模型原位配套，仍保留坡顶必要制动空间。

### 执行与记录

LEVEL立即给参考线形和.5桥的可测试几何，使用已有旋转图元能力，无需等新增引擎字段；CODE尽早做短段真实速度对照，再联合锁定坡；ART依据这些参数原位改当前water-rush.blend和同名资源。首张布局/数据优先于长报告，三锤、十字和大升降本次保持，不再扩大范围。手机开发和已过触控用例继续保留。

暂停把上一轮challenge全程作为本次最终验收；已经有效的手机输入、旧几何实测保留，避免重做。已有challenge玩家/测试成绩仍保留，由CODE按现有关卡+规则机制明确新几何归属，不混报或清空；不为调参复制关卡、模型版本或备份。新几何到位后完成相应短对照、窄梁手机控线和完整新路线，旧成绩与旧参数仅作历史依据。

### 用户取消检查前置，按统一候选直接完成开发

用户在代码任务最新指示“不用检查了，继续开发”。CODE 已停止自动测试/试玩并清理其测试输入和临时页面；旧challenge只行至CP2，已有手机细桥与刹车等待证据保存在 `challenge-progress.json`，不写成旧challenge已全通关。

新蛇形规则 `serpentine` 已由代码加入，当前v3按原结构保留standard/challenge归档；旋转图元也支持新坡改向-Z，不存在等待新引擎字段的前置。LEVEL应尽早给手绘蛇形/.5桥/坡的可运行统一几何，CODE直接接入，ART按当前候选原位配套，不再等待新速度对照或最终坡“已调平衡”才交付。必要坐标/字段/模型一致性仍由各负责人在实施中维护，但不重新启动自动测试或试玩循环。

本节覆盖前文要求“先短测再冻结/先通关再完成”的执行顺序。保留这些用例为未执行说明，不删除旧证据，也不声称新坡、蛇形或手机已全面验证。以代码、配置、模型和文档按最新需求实际完成作为本批收口条件，之后由统筹按已授权范围直接add/commit/push main；既有CI仍随推送自动执行。用户并未要求删除CI或改服务器。

最新正式数据交接：LEVEL 回传 TS `947b407f71d7cd107c71501893951468d904884bbab689ad57ed00dc2f65a66e`、JSON `62525d6c3a6dc0fc9d41a4032e4ca994e074398ad1150c1f9ef69bf8e7f764f0`。蛇形为R3.5、宽1.6、四次交替180°回头加入口90°，中间4米直臂，166个旋转台面盒、16个支撑；桥.5及细线同步。末段切向-Z、连接.6米，主坡明确为50°/斜长4.4米（非旧4.25），前后20°/1.25，连带顶台/终点/栏已同步；三锤、十字和升降保持。

这些是用户要求继续开发后的未实测候选，LEVEL没有新跑测试或试玩。ART先前开工记录JSON8c855d75需与这次62525d6c对应的最终几何对齐，不能导出旧坡长度再恢复引用。三方只完成必要数据与模型配套，Git写操作继续由统筹统一。

### 本批开发交付完成，转统一 Git 收口

LEVEL已按ART最终交付恢复同名静态引用：正式TS `a31ce222807ac355893f65d62c35a1ebcaee893775fea0c0cd1e47990d8447b6`，当前JSON `495f64546da3cc62b5a978a28563426d3ee3944d0c1110b9f35afae163a6a691`，ART几何源仍为62525d6c。静态GLB为 `81ceb56321a53c7b97932fa2b69019df82b7f815598744c4e42ee7a6f631d6fc`，源文件 `23ff6837e9ede1f2c5aadd4d5f87e4b371c258e52a3387d0e4102e6126092449`；十字/升降文件与引用不变。模型清理了旧终点附件，保留原位文件，无新版本或备份。

CODE已完成serpentine类型/成绩归属、通用-Z坡接入和手机暂停说明/图标式刹车交互；没有等待新接口。关卡、美术及代码停止本批文件写入，统筹读取Git状态/差异准备提交。没有在用户停止检查后重新运行本地测试、构建或试玩；新蛇形/.5梁/50°4.4坡的可玩性、完整路线和真机手机体验保留未验证边界。旧standard通关、旧R8短对照、旧手机原生触控记录均只按各自适用版本保留。

本次不操作服务器，GitHub现有CI随main推送自动运行。提交范围为当前关联源码/数据/模型/说明及有效历史记录，不回滚或覆盖其他用户更改；如CI产生结果在最终回报中注明。
