# 两阶段开发与main推送执行单

2026-09-14。用户最新明确授权：完成两条已设计赛道后推送远程main；再将最新机关整合成测试赛道并接入游戏，再推送main。用户离开，要求直接执行，不逐次询问。本文件覆盖此前“不Git写入/不推送”和新八机关“仅设计”的阶段限制；不授权删除用户改动、强推或直接改服务器。

## 第一阶段：两条赛道与第一次推送

按 [两关正式开发任务](two-courses-implementation.md) 完成top-difficulty与three-route：现有三关保留、顶级难度命名挑战、三路分流编号04，目录/图鉴来源/分支CP/成绩/模型完整接入。

CODE运行必要测试、npm test、npm run build，验证单路线关键完整进度、第四关A/B/C选路/合流/重生/终点及小屏输入，修复阻塞缺陷，不跳过失败用例或降低用户要求冒充通过。ART/LEVEL配套交付完成再收口。

CODE作为本批唯一Git写入方：重读当前main/远程状态、检查各owner变更与暂存区，只提交本批两关和已确认配套改动/交付文档，排除.pyc等缓存和未知用户文件。保留当前用户提交（本次启动读到HEAD f58dccb）。可以正常fetch更新远程信息；远程领先时保留双方历史和用户改动，不能强推或hard reset。提交后正常push origin main，记录完整commit、推送结果与对应Actions运行；读取本次CI最终状态。已知推送会触发现有发布/部署流水线，属此次用户授权范围，不另请求确认。

第一次推送的关联CI若失败，定位该提交日志、最小修复并补充提交推送，不能明知失败就进入下一批。用户自有未关联修改导致检查失败应准确说明，禁止回滚用户版本。网络/认证不可用时可继续本地必要工作，但不得谎报已推送。

## 第二阶段：八种新机关测试赛道与第二次推送

仅在第一阶段已完成推送并确认对应流水线结果后启动新机关实际开发。依据 `docs/levels/advanced-obstacles.md`、JSON和8图，覆盖全部八项：

- spring-trampoline 蓄能蹦床
- gravity-coaster 重力过山车弯轨
- pulse-jet 脉冲喷气阵
- orbital-catcher 巡航接球斗
- reversing-conveyor 反转输送带桥
- vortex-funnel 回旋漏斗
- gimbal-platform 双轴天平台
- cascade-bridge 连锁坍塌桥

新建独立 `advanced-trial` / **高阶机关试验场**，作为测试赛道接入选关，使用中文“试验”编号槽，不挤占第四关，不覆盖现有成绩。首版规则standard、无未校准奖牌。每项先有可辨入口/恢复区与安全CP，再安排适量新机关连续组合；测试场目标是八项可以实际体验与验证，不是一开始就把全部无缓冲叠成无法定位问题的极限路线。

过山车采用设计已明确的开放落差/倾斜弯首版；完整大环属于后续子方案，不用隐藏磁吸、全局限速修改等强凑完整环。蹦床/喷气的额外作用须有可见机械或喷嘴来源、真实接触或有限体积局部力，不能传送/直接附球。输送带使用真实接触或明确合理的带面物理模型，不只滚贴图；双轴平台/捕球斗/漏斗/坍塌对应真实承托、约束或动态体。玩家基准物理和操控保持，局部新机制参数由设计与真实验证共同收敛。

CODE先实现八项类型/行为和资源接口，LEVEL设计测试场及正式 `src/levels/advanced-trial.ts`，ART制作相应资源。优先独立共享新库 `advanced-obstacle-library.glb`及唯一对应源/manifest（可按实际分件合理组织），测试场静态资源独立，避免覆盖旧五机关库或每个实例重复一个包。具体文件名由CODE/ART协议冻结，不将模板显示当实际机制已实现。

八项接入图鉴，总类数由13扩至21，继续名字/模型/必要控制，预览与真实玩法能力分别标记。测试场选择、暂停/重开、CP落水、结算/成绩隔离、皮肤/纯净/性能兼容完整接入。源码接口和阶段文档不算游戏交付。

本批同样运行必要测试与npm test/npm run build，逐项真实接触/受力、重装/复位、快速碰撞、完整路线和小屏操作验证。问题由对应owner定向修复；不清空用户设置成绩。完成后CODE提交第二批并推送main，读取对应CI结果，必要时修复到通过，记录最终提交与CI链接。

## 协作与执行边界

- 当前三任务先完成第一阶段；第二阶段可以预读设计、列依赖，但实际新模型/玩法写入等到第一次发布关口后统一通知。
- CODE唯一负责add/commit/push及CI技术收口；LEVEL和ART只修改各自职责文件，完成阶段后等待CODE/统筹明确第二阶段开工信号，避免一批提交混入半成品。
- 产品统筹维护本执行单与阶段状态，协调任务间交接；无需求冲突时直接作必要实施决定，用户不在线不视作停止理由。
- 使用现有Git凭据和授权工具，不索要或输出密码/私钥；不安装不明软件、不擅自扩大访问权，不绕过环境拒绝。真正无法继续的外部阻塞记录具体动作与原因。
- PlayCanvas Editor仍是研究方案，本次不上传云端项目、不购买订阅、不把编辑器开发并入发布。
- 缓存/临时测试产物不进入提交；修复只触及任务相关范围，不覆盖用户运行中新增改动。所有“完成/通过/已发布”均需对应代码、工具输出或CI证据。

## 状态与记录

- 第一阶段：开发中，待测试/试玩、commit与第一次push/CI。
- 第二阶段：等待第一阶段关口，尚未开工。
- Git执行授权：**仅本次**两批及其必要修复提交、推送到origin/main已获授权，无需再次确认。用户已明确补充：这不是后续任务的长期Git授权；本次两阶段完成后授权终止，其他任务在用户明确要求前不得add/commit/push、合并/变基、切换分支或执行其他Git写入。不能把无关改动一并提交，后续不得沿用本执行单授权。
- 当前已有两份正式TS和CODE多实例/course协议，LEVEL已补有向gate/steps，待ART资源和CODE测试/实际通路交付；不能将配置落盘视为阶段1完成。
- 持续跟进已设置：当前任务的heartbeat自动化 `marble-lab`，每15分钟检查三任务与阶段关口，无变化静默，两次最终push及关联CI均成功后暂停。CODE仍是唯一Git写入方，跟进不重复创建执行任务。
