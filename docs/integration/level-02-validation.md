# 水上冲关 · 技术验证

当前状态：本地双关已接入，旧 `standard` 完成真实通关。用户随后要求四段调难并增加坡前 S 弯保速，`challenge` 正在配套，下面旧难度证据不能作为新难度通过结论。服务器和发布另行处理。

## 已实现的接入

- `src/game/levels.ts` 注册两关；关卡页可选择，HUD/检查点/纪录读取同一选中项。
- 三把锤使用独立固定长度圆弧、速度及初相位；十字为五个不重叠同步运动学盒体，四角没有整盒/圆柱底板；四升降板与横移台使用真实运动学接触，坡盒在创建刚体前应用真实 Euler 旋转。
- 同一关的同名模型共享请求，多实例各自挂载，卸载统一释放。选关取消未完成请求，迟到资源释放；失败保留基础外观与碰撞。
- `marble-lab-v3` 按关卡及规则存储，旧 v1/v2 原文只读保留，第一关三个旧桶、第二关 standard/challenge 不混排。目标奖牌未校准时第二关仅显示完赛。

## standard 基线（调难前）

正式配置 `c1e3a44092c0191f3e8ee42b6f97c35fc3e3ac0f7746b4de9bf500c9bb12373d`：真实全路线 **88.1256秒 / 1次掉落 / 3个检查点**。使用原键盘事件推进，未写球体位置、速度、检查点或直接完赛。

证据：[开局](level-02-evidence/02-start.png)、[结算](level-02-evidence/04-finished.png)、[完整采样](level-02-evidence/route-first-complete.json)。1039帧、52次接触事件，记录三锤段通行、十字登台/随转/离台、四板逐块承托/接缝、横移失败回CP2后通过、充分助跑上坡与结算。十字约t13.6665开始登台，t14.1029实际接触臂，t19.5392开始对岸离台。测试包含观察等待和控制器选择时机，不用于真人难度或奖牌校准。

第二关池体随后仅扩大无碰撞显示覆盖，配置变为 `b40f12260be40dadd17ccde76e262eded0bb4bd496ade2bb205f9460aea49aca`。LEVEL逐值对比确认6个Pool之外的玩法不变，见[扩大后开局](level-02-evidence/06-expanded-pool-start.png)；第一关保持。

## 难度相关短对照

- **严格低速坡面**：在旧45°坡脚以原按键自然停至 `[5.68035,3.825,-10.09548]`，水平速约 **0.04838m/s**，随后持续A五秒，没有自动减速或隐藏门槛。最高球心Y=5.30117，未到旧坡顶；结束 `[4.08183,4.77212,-10.09177]`。与前述充分助跑上坡构成旧坡对照。证据：[短对照采样](level-02-evidence/standard-targeted-checks.json)、[结束画面](level-02-evidence/08-strict-low-speed-slope.png)。首次88秒路线中的起步0.84样本不算严格静止，不能混用。
- **十字不是只转模型**：复用当前 `createGame` 进行隔离接触实验，仅把测试出生点配置到臂上 `[-6,3.95,13]`，不接收输入、不施加额外力，玩家物理参数不变。角速度0时1.60秒后球稳定在 `[-6,3.825,13]`；角速度.55时发生真实臂接触，球向+X位移约.2855米并在1.5574秒掉落。原生Ammo角速度非零。[无输入对照](level-02-evidence/cross-no-input-comparison.json)。这是接触诊断，不是整关通行或无人控制转乘证明。
- **空角实际落水**：第二次短检查中t14.3937开始用按键走向随台转动的空角，随后t15.6294掉落并回起点；证据仍在 `standard-targeted-checks.json`。球没有被绑定到转台，玩法没有离心推力补丁。

## 验证边界与当前待办

现有自动测试覆盖姿态/角度、五盒空角及拼接、旋转坡面端点、配置/GLB根节点与自包含资源、多实例和取消加载、v1/v2迁移、两关快速切换与challenge保留standard。它们不替代新难度真实试玩。

旧成绩浏览器样本在空白 `localhost:5179` 来源初始化，明确属于测试fixture；玩家 `127.0.0.1:5177` 数据未清除。已有旧三规则显示与第二关空排名验证见 `05-legacy-records.png`。最终新难度仍需补：四段参数/模型配套后的通行、S弯保速与丢速冲坡对照、整关通关、完整资源失败回退/快速切关/刷新保存。第一关只做共享改动的必要回归，其独立美化待办不作为前置。
