# 高阶机关试验场·第二阶段技术验证

2026-09-14。本记录对应 `advanced-trial`、高级八机关共享库和21类障碍图鉴的正式接入。技术证据与用户的完整通关、手感和审美验收分开。

## 自动验证

- `npm test`：84/84 通过。覆盖8类13实例、10 CP/13 gate/23步单路线、资源单次加载与卸载、GLB根与自包含、蹦床触发去重与能量上限、喷流有限体积与阶段力、输送带真实接触、接球斗dock时序、双铰链配置、坍塌触发与重装，以及六关成绩分桶。
- `npm run build`：通过 `vue-tsc --noEmit` 和 Vite 生产构建；仅保留既有大 chunk 提示。
- 开发态只读测试控制器用于观察位姿、路线状态并派发原键盘输入。生产构建不包含 `__marbleLabPlaytest` 字符串，场景卸载会清理控制器。

## 配置与资源

- `src/levels/advanced-trial.ts`：SHA-256 `9b754916f5713f37a681a5b523ca9d3d67b1d0b67c13f92e3627dfe51225a434`。
- `docs/levels/advanced-trial-construction.json`：SHA-256 `a14ecee4d07790928649ab07ae9274dcd9f236aa0e9e3c04af01c507491cb493`。
- `advanced-trial-track.glb`：SHA-256 `137e6b18094491e169de6b06b821c58d500e561d88bbee5354e727b1f9ffab4d`，663,112 字节，35,460 三角面，6材质。
- `advanced-obstacle-library.glb`：SHA-256 `99b0803ec4196cbe2d88170b23b9ce628637205b201a5e7739f8ccf49e40c428`，1,514,564 字节，84,968 三角面，8材质。
- `advanced-obstacle-library.json`：SHA-256 `936894f7d45a9066337951ffb9b35cd6bb0fe302dd1428972f284197a8d5be34`。

静态轨道为唯一 `TrackStatic` 根。高级共享库恰好包含8个identity根，manifest分件路径均存在；两份GLB均为v2、自包含，没有外链buffer或image。静态显示模型、动态机关模型与明确碰撞代理保持分离。

## 隔离浏览器试玩

试玩复用 `127.0.0.1:5177` 项目进程，使用独立 `localhost:5177` origin，未读写玩家 `127.0.0.1` 设置和成绩。测试只派发原有键盘输入并读取开发态位姿，没有写球位置、速度、CP或完赛状态。

- 高阶关的 `TrackStatic` 与13个高级机关实例同时加载，新建页面无应用 warning/error。图鉴显示“全部21、动态机关16、路线挑战5”；已打开蹦床、喷流、接球斗、双轴台、坍塌桥、弯轨和漏斗的放大动态预览。
- 蹦床外缘侧擦不触发。核心落点入射 `vy=-3.028`，一次冲量后离面 `vy=7.858`，低于配置的8.5上限。
- 自动输入从起点推进后在 `t=.918s` 提交CP1、`t=2.960s` 提交CP2。接球斗已真实接住弹珠并随斗移动到dock附近；自动离台仍不稳定。
- 暂停1.2秒时接球斗、双轴内板和坍塌首板的位姿保持不变。重开后 elapsed、falls、CP均清零，机关轨迹重新开始。
- 单路线关的纪录不再保存或显示无意义的 `route: "main"`；三路分流仍只记录A/B/C。

## 边界

本轮没有通过传送弹珠或直接写入进度来伪造通关。尚未取得从起点到结算的整场连续物理通关，因此本记录不证明完整可通性，也不用于设置奖牌阈值。接球斗自动离台、反转输送带图鉴逐卡预览、真机触控和长时间性能仍需后续体验验证。
