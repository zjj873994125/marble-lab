# 两条长关·第一阶段技术验证

2026-09-14。本记录对应 `top-difficulty` 与 `three-route` 首次正式接入，技术证据与产品手感/审美验收分开。

## 自动验证

- `npm test`：69/69 通过。覆盖旧三关回归、多实例坐标/相位、共享模型只请求一次、五关分桶与路线成绩、两份 GLB 根/自包含、top 40 步和 three A/B/C 21/21/20 步全路线验证。
- `npm run build`：通过 `vue-tsc --noEmit` 和 Vite 生产构建。仅保留既有大 chunk 提示。
- 开发态 `window.__marbleLabPlaytest` 只读返回弹珠/动态刚体位姿和路线状态，用于派发原键盘输入的自动试玩。生产构建不包含该名称或实现，场景卸载也会清理。

## 资源与运行

- `top-difficulty-track.glb`：SHA-256 `e763368112829da380e2e3c1f6e1f534125d8e4dc05a8ab8767c9511f98d998b`，5,033,820 字节，268,508 三角面，5 材质。
- `three-route-track.glb`：SHA-256 `f472fdb2e5659594c7b4bb69ae796c2551d8fa33d377722bc1943878d994cb3d`，2,713,168 字节，143,176 三角面，5 材质。
- 两者都是 GLB v2、唯一 `TrackStatic` 根、世界原点、identity、米制 Y-up，buffer/image 无外链。浏览器中基础外观在模型成功后隐藏，动态代理仍独立运行。

## 隔离浏览器试玩

试玩复用 `127.0.0.1:5177` 项目进程，使用独立 `localhost:5177` origin，未读写玩家 `127.0.0.1` 设置和成绩。测试只派发 WASD/Space/R/Escape，没有写球位置、速度、CP 或完赛状态。

- 选关弹窗显示五张完整卡片，“挑战”与“04”正确；两关均加载精细模型，新建干净页面无应用 warning/error。
- top 的定时键盘序列零掉落穿过第一组三锤，在左岸等待十字臂对齐，登臂后随转半周，于 `t=10.77s` 到达右岸 `x=18.055,z=.063`；调整离台时机后于 `t=12.81s` 稳定落到 `x=18.505,z=.022`，全程零掉落。后续真实登上第一块升降板，在第二块接驳失败后按规则掉落重生，证明动态缺口没有隐形固定桥。
- three 完整轨道、公共开场、三支路、合流和共享尾段在首页/挑战镜头可见；公共摆锤和推墙的真实击落/重生已触发。A/B/C 的 ENTRY CP 提交、路线保留、合流门和终点条件由最终配置集成测试逐步通过全部 steps，不允许跳过当前 gate。
- `844x390` 横屏下自动纯净模式只保留计时，`scrollHeight=390`，无游戏页滚动或 HUD 遮挡。触控摇杆/刹车的独立指针、取消和键盘优先级继续由自动测试覆盖；本次没有真机设备证据。

## 边界

本轮没有以传送或写状态伪造物理通关。两条超长关尚未取得从起点到结算的无辅助完整通关样本，因此不用本记录校准奖牌或宣称真人难度已验收。代码/资源/进度协议和关键动态接驳已纳入发布门禁。
